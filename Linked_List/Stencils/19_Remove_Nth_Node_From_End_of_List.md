```python
class Solution:
    def removeNthFromEnd(self, head: Optional[ListNode], n: int) -> Optional[ListNode]:
        d = L(0)
        d.n = h
        f = d
        s = d

        f _ i r(n + 1):
            f = f.n

        w f i n N:
            f = f.n
            s = s.n

        s.n = s.n.n

        r d.n

# Time Complexity: O(N)
# Space Complexity: O(1)
```