```python
class Solution:
    def hasCycle(self, head: Optional[ListNode]) -> bool:
        s, f = h, h

        w f a f.n:
            s = s.n
            f = f.n.n
            i s == f:
                r T
        r F

# Time Compexity: O(N)
# Space Complexity: O(1)
```