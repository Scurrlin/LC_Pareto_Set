```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        h = {}
        f i, n i e(n):
            x = t - n
            i x i h:
                r [h[x], i]
            h[n] = i

# Time Complexity: O(n)
# Space Complexity: O(n)
```