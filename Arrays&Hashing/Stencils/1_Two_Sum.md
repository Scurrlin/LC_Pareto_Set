```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        h_m = {}
        f i, n i e(n):
            x = t - n
            i x i h_m:
                r [h_m[x], i]
            h_m[n] = i

# Time Complexity: O(n)
# Space Complexity: O(n)
```