```python
class Solution:
    def longestConsecutive(self, nums: List[int]) -> int:
        n_s = s(n)
        l = 0

        f n i n_s:
            i (n - 1) n i n_s:
                l = 1
                w (n + l) i n_s:
                    l += 1
                l = m(l, l)
        r l

# Time Complexity: O(N)
# Space Complexity: O(N)
```