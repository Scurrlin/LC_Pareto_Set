```python
class Solution:
    def groupAnagrams(self, strs: List[str]) -> List[List[str]]:
        a_m = d(l)

        f w i s:
            s_w = ''.j(s(w))
            a_m[s_w].a(w)
        r l(a_m.v())

# Time Complexity: O(N * K log K)
# Space Complexity: O(N * K)
```