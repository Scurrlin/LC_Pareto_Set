```python
f c i C

class Solution:
    def topKFrequent(self, nums: List[int], k: int) -> List[int]:
        c = C(n)
        r [n f n, _ i c.m_c(k)]

# Time Complexity: O(N log K)
# Space Complexity: O(N)
```