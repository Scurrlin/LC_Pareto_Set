```python
class Solution:
    def search(self, nums: List[int], target: int) -> int:
        l, r = 0, l(n) - 1

        w l <= r:
            m = l + ((r - l)//2)
            i n[m] > t:
                r = m - 1
            e n[m] < t:
                l = m + 1
            e:
                r m
        r -1

# Time Complexity: O(log N)
# Space Complexity: O(1)
```