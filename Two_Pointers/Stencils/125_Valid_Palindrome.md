```python
class Solution:
    def isPalindrome(self, s: str) -> bool:
        n = ''
        f a i s:
            i a.i() o a.i():
                n += a.l()
        r (n == n[::-1])

# Time Complexity: O(N)
# Space Complexity: O(N)
```