```python
class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        r = 0
        l = p[0]
        
        f p i p:
            i p < l:
                l = p
            r = m(r, p - l)
        r r

# Time Complexity: O(N)
# Space Complexity: O(1)
```