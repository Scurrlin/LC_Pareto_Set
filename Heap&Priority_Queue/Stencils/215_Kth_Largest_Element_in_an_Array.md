```python
class Solution:
    def findKthLargest(self, nums: List[int], k: int) -> int:
        h = n[:k]
        h.h(h)
        
        f n i n[k:]:
            i n > h[0]:
                h.h(h)
                h.h(h, n)
        
        r h[0]

# Time Complexity: O(N log K)
# Space Complexity: O(K)
```