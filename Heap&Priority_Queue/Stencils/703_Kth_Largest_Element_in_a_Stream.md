```python
class KthLargest:
    def __init__(self, k: int, nums: List[int]):
        s.m, s.k = n, k
        h.h(s.m)
        w l(s.m) > k:
            h.h(s.m)
            
    d a(s, v: i) -> i:
        h.h(s.m, v)
        i l(s.m) > s.k:
            h.h(s.m)
        r s.m[0]

# Time Complexity: O(N log N)
# Space Complexity: O(K)
```