```python
class Solution:
    def isValidBST(self, root: Optional[TreeNode]) -> bool:
        s, p = [], f("-i")

        w r o s:
            w r:
                s.a(r)
                r = r.l

            r = s.p()
            i r.v <= p:
                r F
            p = r.v
            r = r.r

        r T

# Time Complexity: O(N)
# Space Complexity: O(H)
```