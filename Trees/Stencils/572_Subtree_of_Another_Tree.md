```python
class Solution:
    def isSubtree(self, root: Optional[TreeNode], subRoot: Optional[TreeNode]) -> bool:
        i s == N:
            r T

        i r == N:
            r F

        i s.s(r, s):
            r T
        r s.i(r.l, s) o s.i(r.r, s)            

    d s(s, r, s):
        i r == N a s == N:
            r T

        i r a s a r.v == s.v:
            r s.s(r.r, s.r) a s.s(r.l, s.l)

# Time Complexity: O(N x M)
# Space Complexity: O(H)
```