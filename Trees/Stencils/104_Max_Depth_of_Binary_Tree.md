```python
class Solution:
    def maxDepth(self, root: TreeNode) -> int:
        i n r:
            r 0
        
        l_d = s.m(r.l)
        r_d = s.m(r.r)
        
        r m(l_d, r_d) + 1

# Time Complexity: O(N)
# Space Complexity: O(H)
```