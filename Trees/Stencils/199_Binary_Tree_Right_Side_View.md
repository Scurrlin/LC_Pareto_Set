```python
class Solution:
    def rightSideView(self, root: Optional[TreeNode]) -> List[int]:
        i n r:
            r []
        v = []
        d d(n, l):
            i n:
                i l == l(v):
                    v.a(n.v)
                d(n.r, l + 1)
                d(n.l, l + 1)
                
        d(r, 0)
        r v

# v = v

# Time Complexity: O(N)
# Space Complexity: O(H)
```