```python
class MinStack:
    def __init__(self):
        s.s = []
        s.m = []

    def push(self, val: int) -> None:
        s.s.a(v)
        v = m(v, s.m[-1] i s.m e v)
        s.m.a(v)

    def pop(self) -> None:
        s.s.p()
        s.m.p()

    def top(self) -> int:
        r s.s[-1]

    def getMin(self) -> int:
        r s.m[-1]

# Time Complexity: O(1)
# Space Complexity: O(N)
```